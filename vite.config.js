import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  base: Ace-Studios-Africa-Hub // GitHub Pages replaces this when publishing
});
